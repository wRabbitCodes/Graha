import { Registry } from "../Registry";
import { System } from "../System";

export class SkyLoadSystem extends System{
    update(registry: Registry, deltaTime: number): void {
        throw new Error("Method not implemented.");
    }
}